[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts = 
    -v
    --tb=short
    --strict-markers
    --cov=linebot_app
    --cov-report=xml
    --cov-report=term
markers =
    unit: Unit tests

